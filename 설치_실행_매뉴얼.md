# VitaSense 설치 및 실행 매뉴얼

> **개발 환경 및 프로덕션 환경 설정 가이드**
> VitaSense 백엔드 서버 컴파일, 설치, 실행 방법

---

## 목차

1. [시스템 요구사항](#1-시스템-요구사항)
2. [사전 준비](#2-사전-준비)
3. [개발 환경 설치](#3-개발-환경-설치)
4. [프로덕션 환경 배포](#4-프로덕션-환경-배포)
5. [실행 및 테스트](#5-실행-및-테스트)
6. [문제 해결](#6-문제-해결)

---

## 1. 시스템 요구사항

### 1.1 하드웨어 요구사항

**최소 사양:**
- CPU: 1 Core
- RAM: 1 GB
- 디스크: 10 GB 여유 공간

**권장 사양:**
- CPU: 2 Core 이상
- RAM: 2 GB 이상
- 디스크: 20 GB 이상 여유 공간

### 1.2 운영체제

**지원 OS:**
- Ubuntu 20.04 LTS / 22.04 LTS (권장)
- Debian 10 이상
- CentOS 8 이상
- macOS 10.15 이상 (개발 환경)
- Windows 10/11 (WSL2 사용 권장)

### 1.3 필수 소프트웨어

| 소프트웨어 | 버전 | 용도 |
|----------|------|------|
| Node.js | 14.x 이상 | 백엔드 서버 |
| Python | 3.7 이상 | PDF 분석 |
| MySQL | 8.0 이상 | 데이터베이스 |
| npm | 6.x 이상 | 패키지 관리 |
| Git | 2.x 이상 | 소스 코드 관리 |

---

## 2. 사전 준비

### 2.1 계정 및 API 키 발급

VitaSense를 실행하기 위해 다음 계정과 키가 필요합니다:

#### 2.1.1 Kakao Developers 설정

1. **Kakao Developers 콘솔 접속**
   - https://developers.kakao.com/console

2. **애플리케이션 생성**
   - "애플리케이션 추가하기" 클릭
   - 앱 이름: VitaSense
   - 사업자명: (선택사항)

3. **REST API 키 발급**
   - 앱 설정 → 앱 키
   - REST API 키 복사 (나중에 `.env`에 사용)

4. **Kakao Login 설정**
   - 제품 설정 → 카카오 로그인 → 활성화 ON
   - Redirect URI 등록:
     - 개발: `http://localhost:4000/kakao/callback`
     - 프로덕션: `http://[서버IP]:4000/kakao/callback`

5. **동의 항목 설정**
   - 제품 설정 → 카카오 로그인 → 동의 항목
   - 필수 동의:
     - 닉네임 (profile_nickname)
     - 카카오톡 메시지 전송 (talk_message)

6. **Client Secret 발급**
   - 제품 설정 → 카카오 로그인 → 보안
   - Client Secret 코드 생성 및 복사

#### 2.1.2 MySQL 데이터베이스 준비

**로컬 개발 환경:**
```bash
# MySQL 설치 (Ubuntu/Debian)
sudo apt update
sudo apt install mysql-server

# MySQL 서비스 시작
sudo systemctl start mysql
sudo systemctl enable mysql

# MySQL 보안 설정
sudo mysql_secure_installation
```

**프로덕션 환경:**
- AWS RDS, Google Cloud SQL 등 매니지드 서비스 사용 권장
- 또는 별도 MySQL 서버 구축

---

## 3. 개발 환경 설치

### 3.1 Node.js 및 npm 설치

#### Ubuntu/Debian
```bash
# NodeSource 저장소 추가 (Node.js 18.x)
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -

# Node.js 설치
sudo apt install -y nodejs

# 버전 확인
node --version   # v18.x.x
npm --version    # 9.x.x
```

#### macOS
```bash
# Homebrew로 설치
brew install node

# 버전 확인
node --version
npm --version
```

#### Windows
- https://nodejs.org/en/download 에서 설치 프로그램 다운로드
- 또는 WSL2 Ubuntu 사용 (권장)

### 3.2 Python 환경 설정

#### Python 3 설치

**Ubuntu/Debian:**
```bash
sudo apt update
sudo apt install -y python3 python3-pip python3-venv
```

**macOS:**
```bash
brew install python3
```

#### Python 가상 환경 생성 (권장)

```bash
# 프로젝트 디렉토리로 이동
cd /path/to/vitasense

# 가상 환경 생성
python3 -m venv venv

# 가상 환경 활성화
source venv/bin/activate  # Linux/macOS
# 또는
venv\Scripts\activate     # Windows
```

### 3.3 프로젝트 클론 및 의존성 설치

#### 3.3.1 Git 클론

```bash
# 공개 저장소인 경우
git clone https://github.com/Graduation-Project-JPD/JPDamn.git
cd JPDamn

# 비공개 저장소인 경우 (Personal Access Token 필요)
git clone https://[YOUR_GITHUB_TOKEN]@github.com/Graduation-Project-JPD/JPDamn.git
cd JPDamn
```

#### 3.3.2 Node.js 의존성 설치

```bash
# npm 패키지 설치
npm install

# 설치된 패키지 확인
npm list --depth=0
```

**주요 패키지:**
- `express`: 웹 프레임워크
- `mysql2`: MySQL 데이터베이스 클라이언트
- `jsonwebtoken`: JWT 인증
- `axios`: HTTP 클라이언트
- `multer`: 파일 업로드
- `node-cron`: 스케줄러
- `cors`: CORS 미들웨어
- `dotenv`: 환경 변수 관리

#### 3.3.3 Python 의존성 설치

```bash
# PDF 분석 라이브러리 설치
cd extract-pdf/extractor

# 방법 1: 가상 환경에서 설치 (권장)
pip install -r requirements.txt

# 방법 2: 시스템 전역 설치 (Ubuntu 22.04+에서는 --break-system-packages 필요)
pip3 install -r requirements.txt --break-system-packages

cd ../..
```

**requirements.txt 내용:**
```
PyMuPDF==1.23.8
easyocr==1.7.0
numpy==1.24.3
opencv-python==4.8.1.78
torch==2.0.1
torchvision==0.15.2
```

**시스템 라이브러리 설치 (Ubuntu/Debian):**
```bash
sudo apt install -y \
  libgl1 \
  libglib2.0-0 \
  libsm6 \
  libxext6 \
  libxrender-dev \
  libgomp1
```

### 3.4 데이터베이스 설정

#### 3.4.1 MySQL 데이터베이스 생성

```bash
# MySQL 접속
mysql -u root -p

# 또는 원격 MySQL
mysql -h [HOST] -u [USER] -p
```

**데이터베이스 생성:**
```sql
-- vitasense 데이터베이스 생성
CREATE DATABASE vitasense CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

-- user_info 데이터베이스 생성
CREATE DATABASE user_info CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

-- 데이터베이스 확인
SHOW DATABASES;

exit;
```

#### 3.4.2 스키마 적용

```bash
# 스키마 SQL 파일 실행
mysql -u root -p vitasense < analysis/VitaSenseDB.session.sql
mysql -u root -p user_info < analysis/VitaSenseDB.session.sql
```

> **참고:** `VitaSenseDB.session.sql` 파일에는 두 데이터베이스의 모든 테이블 스키마가 포함되어 있습니다.

#### 3.4.3 데이터베이스 사용자 생성 (선택사항)

보안을 위해 별도의 데이터베이스 사용자 생성:

```sql
-- MySQL 접속
mysql -u root -p

-- 사용자 생성
CREATE USER 'vitasense'@'localhost' IDENTIFIED BY 'your_secure_password';

-- 권한 부여
GRANT ALL PRIVILEGES ON vitasense.* TO 'vitasense'@'localhost';
GRANT ALL PRIVILEGES ON user_info.* TO 'vitasense'@'localhost';

-- 권한 적용
FLUSH PRIVILEGES;

exit;
```

### 3.5 환경 변수 설정

#### .env 파일 생성

프로젝트 루트 디렉토리에 `.env` 파일 생성:

```bash
nano .env
```

**개발 환경 .env 예시:**

```env
# vitasense Database
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your_mysql_password
DB_NAME=vitasense

# user_info Database
DB_L_HOST=localhost
DB_L_USER=root
DB_L_PASSWORD=your_mysql_password
DB_L_NAME=user_info

# JWT Secret (랜덤 문자열 생성 권장)
JWT_SECRET=your_jwt_secret_key_here_at_least_32_characters

# Kakao API
KAKAO_REST_API_KEY=your_kakao_rest_api_key
KAKAO_REDIRECT_URI=http://localhost:4000/kakao/callback
KAKAO_CLIENT_SECRET=your_kakao_client_secret

# Frontend URL
CLIENT_URL=http://localhost:5173

# Server Port
PORT=4000
```

**JWT_SECRET 생성 방법:**
```bash
# Node.js로 랜덤 문자열 생성
node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
```

저장: `Ctrl+X` → `Y` → `Enter`

### 3.6 초기 데이터 로드 (영양제 데이터)

```bash
# 영양제 데이터 로드
node analysis/main.js
```

이 스크립트는 `analysis/data/supplements_dummy.json` 파일의 영양제 데이터를 데이터베이스에 삽입합니다.

---

## 4. 프로덕션 환경 배포

### 4.1 AWS EC2 + RDS 배포 (권장)

#### 4.1.1 EC2 인스턴스 생성

**1. AWS 콘솔에서 EC2 생성:**
- AMI: Ubuntu Server 22.04 LTS
- 인스턴스 타입: t2.micro (프리티어) 또는 t2.small
- 스토리지: 20 GB gp3
- 보안 그룹 설정:
  - SSH (22): 내 IP
  - HTTP (80): 0.0.0.0/0
  - HTTPS (443): 0.0.0.0/0
  - Custom TCP (4000): 0.0.0.0/0

**2. SSH 키페어 다운로드 및 연결:**
```bash
# 키 권한 설정
chmod 400 vitasense-keypair.pem

# EC2 접속
ssh -i vitasense-keypair.pem ubuntu@[EC2_PUBLIC_IP]
```

#### 4.1.2 EC2 환경 설정

**시스템 업데이트:**
```bash
sudo apt update
sudo apt upgrade -y
```

**Node.js 설치:**
```bash
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt install -y nodejs
```

**Python 및 의존성 설치:**
```bash
sudo apt install -y python3 python3-pip python3-venv
sudo apt install -y libgl1 libglib2.0-0 libsm6 libxext6 libxrender-dev libgomp1
```

**MySQL 클라이언트 설치:**
```bash
sudo apt install -y mysql-client
```

**Git 설치:**
```bash
sudo apt install -y git
```

#### 4.1.3 RDS MySQL 인스턴스 생성

**AWS RDS 콘솔에서:**
1. MySQL 8.0 선택
2. 템플릿: 프리티어
3. DB 인스턴스 식별자: `vitasense-db`
4. 마스터 사용자 이름: `admin`
5. 마스터 암호: 강력한 암호 설정
6. 퍼블릭 액세스: 예
7. VPC 보안 그룹: EC2와 동일한 보안 그룹
8. 초기 데이터베이스 이름: `vitasense`

**보안 그룹 인바운드 규칙 추가:**
- MySQL/Aurora (3306): EC2 보안 그룹 ID

**엔드포인트 복사:**
- 예: `vitasense-db.xxxxx.ap-northeast-2.rds.amazonaws.com`

#### 4.1.4 프로젝트 배포

**프로젝트 클론:**
```bash
cd ~
git clone https://[YOUR_GITHUB_TOKEN]@github.com/Graduation-Project-JPD/JPDamn.git backend
cd backend
```

**Node.js 의존성 설치:**
```bash
npm install
```

**Python 의존성 설치:**
```bash
cd extract-pdf/extractor
pip3 install -r requirements.txt --break-system-packages
cd ../..
```

**.env 파일 생성:**
```bash
nano .env
```

**프로덕션 .env 설정:**
```env
# vitasense Database (RDS)
DB_HOST=vitasense-db.xxxxx.ap-northeast-2.rds.amazonaws.com
DB_USER=admin
DB_PASSWORD=your_rds_password
DB_NAME=vitasense

# user_info Database (RDS - 같은 인스턴스 사용)
DB_L_HOST=vitasense-db.xxxxx.ap-northeast-2.rds.amazonaws.com
DB_L_USER=admin
DB_L_PASSWORD=your_rds_password
DB_L_NAME=user_info

# JWT Secret
JWT_SECRET=your_generated_secret_key

# Kakao API
KAKAO_REST_API_KEY=your_kakao_rest_api_key
KAKAO_REDIRECT_URI=http://[EC2_PUBLIC_IP]:4000/kakao/callback
KAKAO_CLIENT_SECRET=your_kakao_client_secret

# Frontend URL
CLIENT_URL=https://your-frontend-url.com

# Server Port
PORT=4000
```

**RDS 데이터베이스 설정:**
```bash
# RDS에 user_info 데이터베이스 생성
mysql -h vitasense-db.xxxxx.ap-northeast-2.rds.amazonaws.com -u admin -p

# MySQL 프롬프트에서:
CREATE DATABASE user_info CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
SHOW DATABASES;
exit;

# 스키마 적용
mysql -h vitasense-db.xxxxx.ap-northeast-2.rds.amazonaws.com -u admin -p vitasense < analysis/VitaSenseDB.session.sql
mysql -h vitasense-db.xxxxx.ap-northeast-2.rds.amazonaws.com -u admin -p user_info < analysis/VitaSenseDB.session.sql
```

**초기 데이터 로드:**
```bash
node analysis/main.js
```

#### 4.1.5 PM2로 프로세스 관리

**PM2 설치:**
```bash
sudo npm install -g pm2
```

**애플리케이션 시작:**
```bash
pm2 start index.js --name vitasense
```

**PM2 자동 시작 설정:**
```bash
# 부팅 시 PM2 자동 시작
pm2 startup systemd
# 출력된 명령어 실행 (sudo 명령어)

# 현재 PM2 프로세스 저장
pm2 save
```

**PM2 주요 명령어:**
```bash
pm2 status              # 상태 확인
pm2 logs vitasense      # 로그 확인
pm2 restart vitasense   # 재시작
pm2 stop vitasense      # 중지
pm2 delete vitasense    # 삭제
pm2 monit               # 모니터링
```

### 4.2 Nginx 리버스 프록시 설정 (선택사항)

포트 80/443으로 서비스하려면 Nginx 사용:

```bash
# Nginx 설치
sudo apt install -y nginx

# 설정 파일 생성
sudo nano /etc/nginx/sites-available/vitasense
```

**Nginx 설정:**
```nginx
server {
    listen 80;
    server_name your-domain.com;

    location / {
        proxy_pass http://localhost:4000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }
}
```

**Nginx 활성화:**
```bash
sudo ln -s /etc/nginx/sites-available/vitasense /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl restart nginx
```

### 4.3 SSL/TLS 인증서 설정 (HTTPS)

**Let's Encrypt Certbot 사용:**

```bash
# Certbot 설치
sudo apt install -y certbot python3-certbot-nginx

# SSL 인증서 발급
sudo certbot --nginx -d your-domain.com

# 자동 갱신 확인
sudo certbot renew --dry-run
```

---

## 5. 실행 및 테스트

### 5.1 개발 환경 실행

```bash
# 프로젝트 디렉토리로 이동
cd /path/to/vitasense

# 서버 시작
node index.js

# 또는 nodemon 사용 (자동 재시작)
npm install -g nodemon
nodemon index.js
```

**정상 실행 시 출력:**
```
Server is running on port 4000
[Scheduler] Daily alarm generator initialized (00:00 KST)
[Scheduler] Alarm checker initialized (every minute)
```

### 5.2 프로덕션 환경 실행

```bash
# PM2로 시작
cd ~/backend
pm2 start index.js --name vitasense

# 로그 확인
pm2 logs vitasense
```

### 5.3 기능 테스트

#### 5.3.1 서버 상태 확인

```bash
# 로컬
curl http://localhost:4000/

# 프로덕션
curl http://[EC2_PUBLIC_IP]:4000/
```

**예상 응답:**
```json
{
  "redirect": "https://kauth.kakao.com/oauth/authorize?..."
}
```

#### 5.3.2 데이터베이스 연결 확인

```bash
# MySQL 접속 테스트
mysql -h localhost -u root -p vitasense -e "SHOW TABLES;"

# 또는 RDS
mysql -h [RDS_ENDPOINT] -u admin -p vitasense -e "SHOW TABLES;"
```

**예상 출력:**
```
+-----------------------------+
| Tables_in_vitasense         |
+-----------------------------+
| supplement_conditions       |
| supplements                 |
| user_alarms                 |
| user_choices                |
+-----------------------------+
```

#### 5.3.3 영양제 데이터 확인

```bash
mysql -h localhost -u root -p vitasense -e "SELECT COUNT(*) FROM supplements;"
```

데이터가 있어야 정상입니다.

#### 5.3.4 API 엔드포인트 테스트

**HTML 테스트 페이지 사용:**

`vitasense-test.html` 파일을 브라우저로 열고:
1. 서버 URL 입력
2. 서버 상태 확인 클릭
3. 각 기능 테스트

**curl 명령어로 테스트:**

```bash
# 1. 카카오 로그인 URL 받기
curl http://localhost:4000/

# 2. 영양제 추천 조회
curl "http://localhost:4000/supplements?condition=고혈압"

# 3. 건강 판정 테스트
curl -X POST http://localhost:4000/pdf/judge \
  -H "Content-Type: application/json" \
  -d '{
    "체질량지수": 23.5,
    "고혈압_수축기": 120,
    "고혈압_이완기": 80,
    "공복혈당": 95,
    "gender": "남"
  }'
```

### 5.4 로그 모니터링

#### 개발 환경

콘솔 출력 확인:
```
[Scheduler] 2024-12-06 10:30 알람 확인 중...
카카오 로그인 처리 시작
PDF 분석 시작...
```

#### 프로덕션 환경 (PM2)

```bash
# 실시간 로그
pm2 logs vitasense

# 최근 100줄 로그
pm2 logs vitasense --lines 100

# 에러 로그만 보기
pm2 logs vitasense --err
```

### 5.5 성능 모니터링

```bash
# PM2 모니터링 대시보드
pm2 monit

# 메모리 사용량 확인
pm2 status
```

---

## 6. 문제 해결

### 6.1 일반적인 오류

#### 오류 1: "Cannot find module 'express'"

**원인:** npm 패키지가 설치되지 않음

**해결:**
```bash
npm install
```

#### 오류 2: "ECONNREFUSED 127.0.0.1:3306"

**원인:** MySQL 서버가 실행되지 않음

**해결:**
```bash
# MySQL 서비스 시작
sudo systemctl start mysql
sudo systemctl status mysql
```

#### 오류 3: "Unknown database 'vitasense'"

**원인:** 데이터베이스가 생성되지 않음

**해결:**
```bash
mysql -u root -p -e "CREATE DATABASE vitasense CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;"
mysql -u root -p vitasense < analysis/VitaSenseDB.session.sql
```

#### 오류 4: "JWT_SECRET is not defined"

**원인:** .env 파일이 없거나 잘못 설정됨

**해결:**
```bash
# .env 파일 확인
cat .env | grep JWT_SECRET

# 없으면 추가
echo "JWT_SECRET=$(node -e "console.log(require('crypto').randomBytes(32).toString('hex'))")" >> .env
```

#### 오류 5: Python 모듈 import 오류

**원인:** Python 패키지 미설치

**해결:**
```bash
cd extract-pdf/extractor
pip3 install -r requirements.txt --break-system-packages
```

#### 오류 6: "Error: listen EADDRINUSE :::4000"

**원인:** 포트 4000이 이미 사용 중

**해결:**
```bash
# 포트 사용 프로세스 확인
sudo lsof -i :4000

# 프로세스 종료
kill -9 [PID]

# 또는 .env에서 다른 포트 사용
echo "PORT=5000" >> .env
```

### 6.2 데이터베이스 문제

#### 연결 실패

```bash
# MySQL 접속 테스트
mysql -h [HOST] -u [USER] -p

# 방화벽 확인 (AWS 보안 그룹, EC2 iptables 등)
sudo ufw status
```

#### 권한 문제

```sql
-- MySQL에서 권한 부여
GRANT ALL PRIVILEGES ON vitasense.* TO 'your_user'@'%';
GRANT ALL PRIVILEGES ON user_info.* TO 'your_user'@'%';
FLUSH PRIVILEGES;
```

### 6.3 PM2 문제

#### PM2가 재부팅 후 시작되지 않음

```bash
pm2 startup systemd
# 출력된 명령어 실행
pm2 save
```

#### PM2 로그가 너무 큼

```bash
pm2 flush  # 로그 삭제
pm2 install pm2-logrotate  # 로그 로테이션 설치
```

### 6.4 성능 문제

#### 메모리 부족

```bash
# Node.js 메모리 제한 증가
pm2 start index.js --name vitasense --max-memory-restart 500M

# 또는
pm2 delete vitasense
NODE_OPTIONS="--max-old-space-size=1024" pm2 start index.js --name vitasense
```

#### CPU 사용률 높음

```bash
# 프로세스 모니터링
pm2 monit

# 클러스터 모드 사용 (멀티 코어 활용)
pm2 start index.js --name vitasense -i max
```

### 6.5 Kakao API 문제

#### "Invalid redirect_uri"

**해결:**
- Kakao Developers 콘솔에서 Redirect URI 정확히 등록
- `.env`의 `KAKAO_REDIRECT_URI`와 일치하는지 확인

#### "Insufficient scope"

**해결:**
- Kakao Developers 콘솔 → 동의 항목에서 필요한 권한 추가
- `talk_message`, `profile_nickname` 권한 확인

---

## 7. 유지보수

### 7.1 업데이트

```bash
# Git pull로 최신 코드 받기
cd ~/backend
git pull origin main

# 의존성 재설치 (변경된 경우)
npm install

# 서버 재시작
pm2 restart vitasense
```

### 7.2 백업

**데이터베이스 백업:**
```bash
# 로컬
mysqldump -u root -p vitasense > vitasense_backup_$(date +%Y%m%d).sql
mysqldump -u root -p user_info > user_info_backup_$(date +%Y%m%d).sql

# RDS
mysqldump -h [RDS_ENDPOINT] -u admin -p vitasense > vitasense_backup_$(date +%Y%m%d).sql
mysqldump -h [RDS_ENDPOINT] -u admin -p user_info > user_info_backup_$(date +%Y%m%d).sql
```

**복구:**
```bash
mysql -u root -p vitasense < vitasense_backup_20241206.sql
```

### 7.3 로그 관리

```bash
# PM2 로그 확인
pm2 logs vitasense

# 로그 파일 위치
ls ~/.pm2/logs/

# 오래된 로그 삭제
pm2 flush
```

---

## 8. 추가 리소스

### 8.1 공식 문서

- **Node.js:** https://nodejs.org/docs
- **Express.js:** https://expressjs.com
- **MySQL:** https://dev.mysql.com/doc
- **PM2:** https://pm2.keymetrics.io/docs
- **Kakao Developers:** https://developers.kakao.com/docs

### 8.2 GitHub 저장소

- **VitaSense:** https://github.com/Graduation-Project-JPD/JPDamn

### 8.3 문의

- **이메일:** support@vitasense.com
- **이슈 트래커:** GitHub Issues

---

## 9. 체크리스트

설치 및 실행 전 확인 사항:

**사전 준비:**
- [ ] Node.js 14.x 이상 설치
- [ ] Python 3.7 이상 설치
- [ ] MySQL 8.0 이상 설치 또는 RDS 준비
- [ ] Kakao Developers 앱 생성 및 키 발급
- [ ] Git 설치

**설치:**
- [ ] 프로젝트 클론 완료
- [ ] `npm install` 완료
- [ ] Python 패키지 설치 완료
- [ ] `.env` 파일 생성 및 설정
- [ ] MySQL 데이터베이스 생성
- [ ] 스키마 적용 완료
- [ ] 초기 데이터 로드 완료

**실행:**
- [ ] 서버 정상 시작
- [ ] 데이터베이스 연결 확인
- [ ] API 엔드포인트 테스트 통과
- [ ] 카카오 로그인 작동 확인

**프로덕션 (선택):**
- [ ] PM2 설치 및 설정
- [ ] 보안 그룹 설정
- [ ] Nginx 설정 (선택)
- [ ] SSL 인증서 설치 (선택)

---

**설치 및 실행에 성공하셨다면, VitaSense가 정상적으로 작동합니다! 🎉**

문제가 발생하면 [문제 해결](#6-문제-해결) 섹션을 참고하세요.
